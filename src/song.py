class Song:

    def __init__(self, name, artist ): #<artist? instances go in here
        
        self.name = name
        self.artist = artist    ## <<< instances of the song class (always the same instances but the song name and artist change. instance = properties)
        

    
